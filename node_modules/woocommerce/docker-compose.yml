version: '2'

services:
  v4:
    command: npm test
    volumes:
      - ./lib:/usr/src/app/lib
      - ./test:/usr/src/app/test
    build:
      context: .
      dockerfile: "v4.docker"
  v6:
    command: npm test
    volumes:
      - ./lib:/usr/src/app/lib
      - ./test:/usr/src/app/test
    build:
      context: .
      dockerfile: "v6.docker"
  v7:
    command: npm test
    volumes:
      - ./lib:/usr/src/app/lib
      - ./test:/usr/src/app/test
    build:
      context: .
      dockerfile: "v7.docker"
